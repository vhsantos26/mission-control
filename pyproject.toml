[project]
name = "mission-control"
version = "0.2.0"
description = "Local dashboard for Claude Code session analytics"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Victor Hugo dos Santos" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
