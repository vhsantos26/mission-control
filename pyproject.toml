[project]
name = "mission-control"
version = "0.6.0"
description = "Local dashboard for Claude Code + Codex session analytics"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Victor Hugo dos Santos" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
